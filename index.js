/**
 * Demo Node.js project
 * This is a simple placeholder file demonstrating basic Node.js functionality
 */

/**
 * A placeholder function that returns a greeting message
 * @param {string} name - The name to greet
 * @returns {string} - The greeting message
 */
function greet(name) {
    return `Hello, ${name}! Welcome to Node.js`;
}

// Example usage
console.log(greet('Developer'));
